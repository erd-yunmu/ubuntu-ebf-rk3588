#!/bin/sh

PREREQ=""

prereqs()
{
	echo "$PREREQ"
}

case "$1" in
prereqs)
	prereqs
	exit 0
	;;
esac

. /scripts/functions

if read_fstab_entry /boot/firmware ext4; then
	BOOTDEV_RAW="$MNT_FSNAME"
	BOOTDEV=$(resolve_device "$BOOTDEV_RAW") || BOOTDEV=""

	if [ -n "$BOOTDEV" ] && [ -b "$BOOTDEV" ]; then
		checkfs "$BOOTDEV" /boot/firmware ext4
	else
		log_warning_msg "Unable to resolve boot filesystem device $BOOTDEV_RAW; skipping check."
	fi
else
	log_warning_msg "No ext4 /boot/firmware entry found in fstab; skipping boot filesystem check."
fi
